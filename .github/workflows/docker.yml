name: Docker
on:
  push:
    tags:
      - 'ndn-tools-*'
  schedule:
    # twice a month
    - cron: '20 8 5,20 * *'
  workflow_dispatch:

permissions:
  packages: write
  id-token: write

jobs:
  ndn-tools-build:
    uses: named-data/actions/.github/workflows/docker-image.yml@v1
    with:
      name: ndn-tools
      target: ndn-tools
